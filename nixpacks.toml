[phases.setup]
nixPkgs = ["python312", "tesseract", "libGL"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -c \"from app.db.init_db import init_db; init_db()\" || true"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
